import SearchInput from './SearchInput';
import Filter from './Filter';

function SearchBox () {
    return (
        <div>
            <Filter/>
            <SearchInput/>
        </div>
    );
}

export default SearchBox;

